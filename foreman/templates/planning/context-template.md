# Issue {{X}} Context — Release {{X}}, Sprint {{N}}

> Generated by **context-manager**. This document is the **Bible** for subsequent agents.

---

## Issue Overview

<!-- PLACEHOLDER: replaced by context-manager -->

- **Issue**: {{ISSUE_ID}} — {{ISSUE_TITLE}}
- **State**: {{ISSUE_STATE}}
- **Summary**: {{ISSUE_SUMMARY}}
- **Labels**: {{ISSUE_LABELS}}
- **Assignees**: {{ISSUE_ASSIGNEES}}
- **Projects**: {{ISSUE_PROJECTS}}

---

## Issue Specific Epics

**Implementing Concepts:** [{CONCEPT_IDS}] <!-- Trace back to concept-checklist.md -->

**Capability Statements**

- {CAPABILITY_1}: {STATEMENT}
- {CAPABILITY_2}: {STATEMENT}
- {CAPABILITY_N}: {STATEMENT}

**Success Metrics**

- For {CAPABILITY_1}: {MEASURABLE_OUTCOME}
- For {CAPABILITY_2}: {MEASURABLE_OUTCOME}
- For {CAPABILITY_N}: {MEASURABLE_OUTCOME}

---

## Codebase Context

**Implementation Patterns Discovered:**

- **From CLAUDE.md memories**: Existing patterns documented in relevant folder memories
- Pattern discovery and analysis from codebase examination
- Existing component and service patterns that can be leveraged
- Architectural constraints and integration points

**Issue Integration Points:**

- How this issue integrates with existing codebase patterns
- Reusable components, services, and utilities identified
- Constraints and considerations from current implementation
- **Memory pattern updates**: New patterns discovered that should be added to CLAUDE.md files

---

## Decision Matrix

> **Authoritative representation is now per‑agent YAML blocks.**  
> The context‑manager writes one YAML block **per relevant agent** (e.g., `backend`, `frontend`).  
> All decision rows, rules, scope of work, impact/test plans, and pattern analyses live inside these YAML blocks.

### Decision Matrix — YAML Blocks (one per agent; **no code**, instructions only)

> **How to use:** For each concrete decision required to implement this issue, add one entry under `decisions`.  
> Keep scope tight and testable. Cite evidence and rules.  
> For every decision, entries in `files_and_globs` **must** be mirrored into the global **Scope of Work** section (managed automatically by the context‑manager).

```yaml
agent: backend
agent_version: v1
decisions:
  - dec_id: DM-001
    concepts: ["CONCEPT-001", "CONCEPT-003"] # Which concepts this decision supports
    driver: "{{Issue # / AC ref}}"
    options_considered: ["A: …", "B: …"]
    decision: "{{Chosen option}}"
    rationale_and_evidence: "Why chosen; cite components, endpoints, schemas, diffs"
    rules_applied:
      - id: SEC-01
        level: MUST
      - id: LOG-02
        level: SHOULD
    areas: ["api", "db"] # one or more of: api|ui|db|infra
    tests_required: ["unit", "contract"] # unit|integration|contract|BDD
    risk: M # L|M|H
    owner: "{{name}}"
    due: "{{YYYY-MM-DD}}"
    revisit_criteria: "e.g., perf SLO not met"
    narrative:
      impact: "What changes upstream/downstream"
      compatibility: "breaking? mitigation?"
      perf_security: "budgets, checks"
      follow_ups: ["ticket-123", "ticket-456"]

scope_of_work: # authoritative, add as many that are needed.
  implementation_decisions:
    files_to_modify:
      - path: "{{PATH/TO/FILE}}"
        purpose: "{{PURPOSE}}"
        rationale: "{{WHY}}"
    files_to_create:
      - path: "{{PATH/TO/FILE}}"
        purpose: "{{PURPOSE}}"
        rationale: "{{WHY}}"
    component_to_extend:
      - path: "{{PATH/TO/COMPONENT}}"
        purpose: "{{PURPOSE}}"
        rationale: "{{WHY}}"
    new_endpoints:
      - method: "POST"
        url: "/v1/orders"
        controller_method: "OrderController.create"
        follows_pattern: "existing-pattern-ref"
    database_changes:
      - object: "orders table"
        change: "add"
    integration_points:
      - service: "Existing service name"
        integration: "how to integrate"

decision_rules_ledger:
  - rule_id: SEC-01
    path: "{{PATH/TO/RULE}}"
    summary: "No secrets in logs"
    enforcement: MUST
    applies_to: ["DM-001", "DM-003"]
    notes: "…"
  - rule_id: LOG-02
    path: "{{PATH/TO/RULE}}"
    summary: "Structured logs with requestId"
    enforcement: SHOULD
    applies_to: ["DM-001"]
    notes: "…"

impact_matrix: # single source of truth for impacted entities (subset for this agent)
  - entity: "GET /v1/users"
    type: "API" # API|Event|DB|UI|Job|Pipeline
    direction: "upstream" # upstream|downstream
    contract_or_schema: "OpenAPI v2.3"
    change: "modify" # add|modify|remove
    breaking: true
    mitigation: "version bump + adapter"
    owner: "Team A"
    risk: "high"
    tests_required: ["contract", "e2e"]
    docs_required: true
    evidence_links: ["link-to-diff"]

test_plan:
  - area: "API: /v1/users"
    test_type: "contract"
    command: "npm run test:contract -- /users"
    env_flags: "ENV=dev"
    owner: "QA"
    notes: "ensure version bump"

cross_cutting:
  security_privacy:
    threat_model_delta: false
    secrets_handling_storage: true
    dependency_licensing_sca_delta: false
    regional_residency: false
  observability:
    structured_log_keys: true
    trace_spans_attributes: true
    sli_slo_changes_alerts: false
    error_taxonomy: true
  change_safety:
    feature_flags_kill_switches: true
    backward_compat_adapters: true
    rollback_plan_owner: true
    stakeholder_comms: true

relations_map:
  flows: [] # optional, add flow identifiers
  screens: []
  endpoints: ["/v1/users"]
  entities: ["User"]
  pipelines: []

architecture_analysis:
  detected_stack:
    framework: "specific framework list"
    ui_library: "detected library or 'Custom Components'"
    styling: "detected approach with examples"
    state_management: "detected solution with store location"
    routing: "detected router with route patterns"
    database: "database type and version if available"
    forms: "detected approach with validation patterns"
    orm_odm: "specific ORM/ODM"
    authentication: "auth method"
    validation: "validation approach"
    typescript: "Yes/No with strictness level"
    testing: "detected framework with test patterns"
  existing_patterns:
    api_structure: "describe pattern"
    endpoint_format: ["URL pattern example 1", "example 2"]
    controller_pattern: "method organization"
    service_pattern: "business logic organization"
    error_handling: "error response format"
    data_validation: "validation approach"
  reusable_components:
    - name: "Component"
      location: "path/to/component"
      purpose: "Purpose"
  codebase_conventions:
    file_naming: "detected pattern with examples"
    linting: "detected linting requirements"
    component_structure: "functional/class with hooks patterns"
    export_style: "default/named with consistency %"
    directory_organization: "feature/layer-based"
    test_location: "detected pattern"
  finality: "NO QUESTIONS - ALL DECISIONS FINAL"

IMPLEMENTATION READY: YES
```
